import { Button } from "~/components/ui/button";

export default function NextButton() {
  return (
    <Button className="w-full h-12 lg:h-14 rounded-full border-2 border-foreground bg-accent text-lg lg:text-xl font-semibold">
      다음
    </Button>
  );
}
